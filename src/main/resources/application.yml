spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_compass
    username: root
    password: 6363
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql

