spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/student_compass
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql

